module HttpCheckCurrent;


export public class HttpCheckCurrent {
       public static void checkCurrent(int check, int current)(ref check, ref current) {
              return HttpCheckCurrent.checkCurrent(check, current);
       }
       public static void checkEventClick(int check, char event, char click)(ref check, ref event, ref click){
              return HttpCheckCurrent.checkEventClick(check, event, click);
       }
       public static void checkClick(int check, short click)(ref check, ref click){
              return HttpCheckCurrent.checkClick(check, click);
       }
       
}

   
